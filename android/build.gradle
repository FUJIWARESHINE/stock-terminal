buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.4'
    }
}

allprojects {
    // 仓库统一由 settings.gradle 的 dependencyResolutionManagement 提供
}

tasks.register('clean', Delete) {
    delete rootProject.buildDir
}
